{
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "scripts": {
    "start": "concurrently \"npm run server\" \"npm run flutter\"",
    "server": "cd functions && npm start",
    "flutter": "flutter run -d chrome"
  },
  "dependencies": {
    "firebase": "^11.8.1"
  }
}
